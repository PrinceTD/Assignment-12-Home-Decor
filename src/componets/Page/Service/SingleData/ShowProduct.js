import React from 'react';

const ShowProduct = (props) => {
    const {img, name, details, price} = props.singleProduct
    return (
        <div>
            
        </div>
    );
};

export default ShowProduct;